# DEPLOYMENT

## Development Setup
1. Clone the repository.
2. (Future) Install dependencies: `npm install`.
3. (Future) Run development server: `npm run dev`.

## Production Deployment
(To be determined as the architecture evolves)
